const logDrop = document.getElementById('logDrop');
const logDropMenu = document.getElementById('logDropMenu');

logDrop.addEventListener('click', () => {
  logDropMenu.classList.toggle('hidden');
});
